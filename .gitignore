# --- Local secrets / credentials (never commit) ---
databricks token
*.pat
.databrickscfg
**/codeengine/*.local.js
# Bundled Lakebase pg client (contains embedded M2M service-principal secret)
scripts/lakebase_pg_bundle.b64

# --- Local tooling / editor / OS ---
.claude/
.vscode/
.DS_Store
Thumbs.db

# --- Installed skill libraries (third-party, not project artifacts) ---
.cursor/skills/
.ai-dev-kit/

# --- Node ---
node_modules/
npm-debug.log*

# --- Local Python environments ---
.venv/

# --- Scratch / local working artifacts (not part of the app) ---
screencapture-*.png
domo-databricks-reskin-kit/
domo-databricks-reskin-kit.zip
analyzer.html
pattern4-agent-portal/**/dbx-domo-logo.png
